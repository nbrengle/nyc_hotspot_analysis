package finalproject

import com.github.tototoshi.csv._
//import finalproject.LineParser

object Main {
    def main(args: Array[String]) {

        //add a loop pull the references from HDFS:
        // val reader = CSVReader.open(new File("")) //I need the filename here
        // val events = reader.foreach(fields => LineParser.parseLine(fields))
        // val matrix = EventAccumulator(events).collect() // need to import spark...

    }
}
